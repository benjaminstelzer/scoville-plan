# Case 06 report

The copied native Plan Goal was shortened without changing its stated outcome: preserve all existing cached reports when migrating to schema 2.

Only `verbose-before/docs/plans/0001-migrate-cli-cache.md` changed. The read-only profile validator passed (`valid: true`; 0 errors, 0 warnings); the scoped diff contains only the Goal replacement. See `ledger.md` for the actual read order, prior-turn-fact treatment, decision, and validation record.
